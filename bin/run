#!/usr/bin/env node

/**
  * Module dependencies.
  */
var debug = require('debug')('app:web');
var _ = require('../lib/server/config');

// app

var app = require('../lib/server');

// bind

app.listen(config.port);

debug('started app on port %d', config.port);
